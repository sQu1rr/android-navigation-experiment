<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/helloWorldFragment">

    <fragment
        android:id="@+id/helloWorldFragment"
        android:name="com.example.navigationexperiment.HelloWorldFragment"
        android:label="fragment_hello_world"
        tools:layout="@layout/fragment_hello_world" >
        <argument
            android:name="arg"
            app:argType="integer"
            android:defaultValue="1" />
    </fragment>
</navigation>